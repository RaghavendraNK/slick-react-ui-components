import React from "react";
import { Button as SlickBtton } from "slick-react-ui-components";
const Button = () => {
  return (
    <SlickBtton
      id="ds"
      dense={32}
      label="Button"
      onClick={() => {}}
      variant="filled"
    />
  );
};
export default Button;
